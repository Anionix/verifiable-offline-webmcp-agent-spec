# Wolfram reference model

`ReferenceModel.wl` contains symbolic/reference functions for policy thresholds, retry geometry, reliability, and probability mass. `verification-report.json` records formulas evaluated through a Wolfram Language kernel during artifact generation.

Production code does not depend on Wolfram; the model acts as an independent mathematical oracle.
